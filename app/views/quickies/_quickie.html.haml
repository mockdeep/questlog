.row-fluid
  = form_for(quickie, method: :delete, html: { id: 'delete-form' }) do
    %h2#quickie{ class: (quickie.over_skipped? ? 'over_skipped' : '') }
      %i.icon-remove#delete-quickie{ title: 'delete quickie' }
      %span.title{ title: "skip count: #{quickie.skip_count}" }= quickie.title
      %i.icon-arrow-down#edit-quickie{ title: 'edit quickie' }
.row-fluid#buttons
  .span6
    .row-fluid
      = button_to('Done! Give me another!', quickie_path(quickie, quickie: { done: true }), method: :put, class: 'btn btn-primary btn-large btn-block')
  .span6
    .row-fluid
      = button_to('Skip this one for now.', quickie_path(quickie, quickie: { skip: true }), method: :put, class: 'btn btn-info btn-large btn-block')
%hr
