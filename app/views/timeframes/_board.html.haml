%header.timeframes-header
  %h2
    Median Productivity: #{ToEnglish.seconds(median_productivity)} per day

- spaces = timeframe_spaces(timeframes)
- timeframes.reject { |frame| frame.tasks.empty? }.each do |timeframe|
  = render 'timeframes/section', timeframe:, spaces:
