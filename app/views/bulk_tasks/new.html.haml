= form_with(scope: :bulk_task, url: bulk_task_path, method: :post) do |form|
  .row
    .col-md-offset-3.col-md-6
      = form.text_area(:titles, class: 'task-input', rows: 15, required: true)
  .row
    .col-md-offset-3.col-md-6
      = form.submit('Add Tasks', class: 'btn btn-success btn-block')
