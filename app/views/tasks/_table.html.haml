- if tasks.any?
  %div{ id: table_id }
    %table.tasks-table
      = render Views::Tasks::TableHeaders.new(heading:, spaces: nil)
      %tbody
        - tasks.each do |task|
          = render Views::Tasks::Row.new(task:, status:, spaces: nil,
            draggable:)
