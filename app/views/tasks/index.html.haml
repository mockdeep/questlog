#current-tasks
  %h2 Current Tasks

  %ul
    - @tasks.each do |task|
      = render 'line_item', task: task

#pending-tasks
  %h2 Pending Tasks

  %ul
    - @pending_tasks.each do |task|
      = render 'line_item', task: task
