#!/usr/bin/env bash

set -euo pipefail

pnpm esbuild app/javascript/*.ts \
  --bundle \
  --format=iife \
  --loader:.png=dataurl \
  --outdir=app/assets/builds \
  --sourcemap \
  --target=es2015 \
  --tree-shaking=true \
  --public-path=/assets \
  "$@"
