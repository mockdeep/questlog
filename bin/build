#!/usr/bin/env bash

set -euo pipefail

pnpm esbuild app/javascript/*.ts \
  --bundle \
  --format=iife \
  --outdir=app/assets/builds \
  --sourcemap \
  --target=es2015 \
  --tree-shaking=true \
  "$@"
